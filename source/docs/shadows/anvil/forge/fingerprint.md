# Fingerprint construction

* Static matchers: asset hashes, headers, banners, protocol constants
* Metadata tagging: unsafe or sensitive artefacts flagged (from lab)
* Device type, vendor, firmware version attached
* Optional transformation for automated scanners (DSL or JSON formats)